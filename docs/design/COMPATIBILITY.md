# Compatibility

Prefer BaseLib-supported extension points over broad patches.

Before implementing a mechanic, inspect current BaseLib and RitsuLib sources when they are available locally. Prefer existing hooks, helpers, and supported extension points over local reimplementation, and document when a library is absent or insufficient.

Before adding Harmony patches, document why a supported API is insufficient and what shared game behavior the patch touches.
