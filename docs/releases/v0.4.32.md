# Elesis v0.4.32

- Fixed Elesis specialization thresholds not opening after XP rewards when returning to the map through reward-screen flows that do not emit a room-entry hook.
- Added a targeted `RunManager.ProceedFromTerminalRewardsScreen()` postfix that checks Elesis progression only after the terminal reward transition completes.
- Kept specialization and evolution events deferred until rewards are finished, avoiding interruption during reward collection.
