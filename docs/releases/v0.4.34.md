# Elesis v0.4.34

- Fixed boss/end-of-act progression events opening while the next-act transition was still stabilizing.
- Removed the `RunManager.ActEntered` progression check because it can fire too early during act transitions.
- Added a targeted `RunManager.EnterNextAct()` postfix so Elesis progression is checked after the next-act transition task completes.
