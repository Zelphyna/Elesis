# Elesis v0.4.59

- Changes Counter Attack expiration to use the same combat timing as normal Block cleanup.
- Adds dedicated Burn and Counter Attack power icons in packed and big sizes.
- Wires `BurnPower` and `CounterAttackPower` to use their dedicated icons instead of the shared placeholder.
