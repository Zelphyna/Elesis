# Elesis v0.4.67

- Includes the authorized converted TDA Empire Sword runtime model and textures in the mod package.
- Makes the skeletal `idle` and sword `attack` clips available from a normal clone or release install.
- Retains the animated 2D combat visual as a defensive fallback if the 3D model cannot be loaded.
- Keeps the original BowlRoll archive, PMX source files, and Blender working files out of the repository.
