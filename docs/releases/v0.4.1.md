# Elesis v0.4.1

Character selection reliability fix.

- Switched Elesis from `PlaceholderCharacterModel` to `CustomCharacterModel`, matching the working Hologirl mod pattern.
- Added the required `GetArchitectAttackVfx()` implementation for the custom character model.
- Documented why Elesis should not use `PlaceholderCharacterModel`.
- Removed obsolete `explication.md`.
