# Elesis v0.4.27

- Restored a lightweight `RunManager.RoomEntered` fallback for Elesis specialization threshold checks.
- Removed the old node-count gate from specialization triggering so thresholds are checked on every map arrival.
- Made pending specialization/evolution thresholds retryable when a previous event attempt did not complete.
- Kept the relic model hook path as an additional trigger for STS2 builds where it fires reliably.
