# Elesis v0.4.29

- Fixed specialization threshold events not opening after XP rewards when STS2 remains in the reward flow instead of entering a `MapRoom`.
- Let `ElesisExperienceReward` trigger progression checks immediately after granting XP.
- Added a non-map progression check mode for reward-driven threshold openings while keeping map-entry checks as a fallback.
